# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#
# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

# ---------
# VARIABLES
# ---------

# Workspaces
set $wp1 "1"
set $wp2 "2"
set $wp3 "3"
set $wp4 "4"
set $wp5 "5"
set $wp6 "6"
set $wp7 "7"
set $wp8 "8"

set $dark   #383c4a
set $light  #7c818c
set $active #5294e2
set $urgent #5294e2

# Mod4 = Windows or super key
# Mod1 = Left Alt-key
set $mod Mod4
set $alt Mod1

# ----------------------
# AUTOSTART AND DEFAULTS
# ----------------------

exec_always --no-startup-id nitrogen --restore; sleep 1; compton -b
exec --no-startup-id google-chrome --app=https://stream.telenor.se
# exec --no-startup-id pavucontrol

# Use xprop in terminal to find window classes
for_window [class="XTerm"] fullscreen enable
for_window [class="^.*"] border pixel 1

# Start program on predefined workspace
# assign [class="Google-chrome"] $wp3
# assign [class="Postman"] $wp4
# assign [class="Code"] $wp2
# assign [class="Pavucontrol"] $wp2

# thin borders
hide_edge_borders both

# Set default workspace layout
# workspace_layout <default|stacking|tabbed>
workspace_layout tabbed

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
# font pango:DejaVu Sans Mono 8
# font pango:monospace 9

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# --------------------------------------
# KEYBINDINGS USING: Super / Windows Key
# --------------------------------------

bindsym $mod+Return exec --no-startup-id lxterminal -e "bash -c echo '' && neofetch && echo 'Debian version: $(cat /etc/debian_version)' && echo '' && bash"
bindsym $mod+space exec --no-startup-id rofi -show run
bindsym $mod+Tab exec --no-startup-id rofi -show window
bindsym $mod+z exec --no-startup-id rofi -show ssh

bindsym $mod+a exec --no-startup-id lxappearance
bindsym $mod+b exec --no-startup-id stacer
bindsym $mod+c exec --no-startup-id mousepad $HOME/.config/i3/config
bindsym $mod+d exec --no-startup-id google-chrome --app=https://stream.telenor.se
bindsym $mod+e exec --no-startup-id pavucontrol
bindsym $mod+f exec --no-startup-id pcmanfm
bindsym $mod+p exec --no-startup-id shotwell
bindsym $mod+g exec --no-startup-id google-chrome
bindsym $mod+m exec --no-startup-id nvidia-settings
bindsym $mod+n exec --no-startup-id nitrogen
bindsym $mod+s exec --no-startup-id spotify
bindsym $mod+v exec --no-startup-id xterm -e cmatrix

# kill focused window
bindsym $mod+q kill

# change focus
bindsym $mod+l focus right
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+i focus up

# alternatively, you can use the cursor keys:
bindsym $mod+Right focus right
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up

# switch to workspace
bindsym $mod+1 workspace $wp1
bindsym $mod+2 workspace $wp2
bindsym $mod+3 workspace $wp3
bindsym $mod+4 workspace $wp4
bindsym $mod+5 workspace $wp5
bindsym $mod+6 workspace $wp6
bindsym $mod+7 workspace $wp7
bindsym $mod+8 workspace $wp8

# -----------------------------------
# KEYBINDINGS USING: Super + Left Alt
# -----------------------------------

# split in horizontal orientation
bindsym $mod+$alt+h split h

# split in vertical orientation
bindsym $mod+$alt+v split v

# move focused window
bindsym $mod+$alt+i move right
bindsym $mod+$alt+j move left
bindsym $mod+$alt+k move down
bindsym $mod+$alt+l move up

# alternatively, you can use the cursor keys:
bindsym $mod+$alt+Right move right
bindsym $mod+$alt+Left move left
bindsym $mod+$alt+Down move down
bindsym $mod+$alt+Up move up

# ------------------------------------
# KEYBINDINGS USING: Super + Left Ctrl
# ------------------------------------

# Cycle through active workspaces
bindsym $mod+Ctrl+Right workspace next
bindsym $mod+Ctrl+Left workspace prev

# focus the parent container
bindsym $mod+Ctrl+Up focus parent

# focus the child container
#bindsym $mod+Ctrl+Down focus child

# reload the configuration file
bindsym $mod+Ctrl+9 reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Ctrl+0 restart

# move focused container to workspace
bindsym $mod+Ctrl+1 move container to workspace $wp1
bindsym $mod+Ctrl+2 move container to workspace $wp2
bindsym $mod+Ctrl+3 move container to workspace $wp3
bindsym $mod+Ctrl+4 move container to workspace $wp4
bindsym $mod+Ctrl+5 move container to workspace $wp5
bindsym $mod+Ctrl+6 move container to workspace $wp6
bindsym $mod+Ctrl+7 move container to workspace $wp7
bindsym $mod+Ctrl+8 move container to workspace $wp8

# enter fullscreen mode for the focused container
bindsym $mod+Ctrl+f fullscreen toggle

# change container layout (stacked, tabbed, toggle, splith, splitv)
bindsym $mod+Ctrl+Return layout toggle
bindsym $mod+Ctrl+s layout stacking
bindsym $mod+Ctrl+w layout tabbed
bindsym $mod+Ctrl+a layout splitv
bindsym $mod+Ctrl+d layout splith

# toggle tiling / floating
bindsym $mod+Ctrl+q floating toggle

# change focus between tiling / floating windows
bindsym $mod+Ctrl+e focus mode_toggle

# ----------------
# SPECIAL BINDINGS
# ----------------

# Readme: https://faq.i3wm.org/question/3747/enabling-multimedia-keys/?answer=3759#post-id-3759

# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume 0 +5% #increase sound volume
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume 0 -5% #decrease sound volume
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle # mute sound

# Screen brightness controls
bindsym XF86MonBrightnessUp exec xbacklight -inc 20 # increase screen brightness
bindsym XF86MonBrightnessDown exec xbacklight -dec 20 # decrease screen brightness

# Media player controls
bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioPause exec playerctl pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

# System
bindsym Ctrl+mod1+Delete exec systemctl reboot
bindsym Ctrl+mod1+Escape exec systemctl poweroff

# window colors
#                               border              background         text                 indicator
client.focused                  $active             $active            $dark                $active
client.unfocused                $dark               $dark              $light               $active
client.focused_inactive         $dark               $dark              $light               $active
client.urgent                   $urgent             $urgent            $dark                $active

# Start i3bar to display a workspace bar and time

bar {
    status_command i3status -c $HOME/.config/i3/i3status.conf
    font pango:monospace 14
    mode hide
    hidden_state hide
    modifier $mod

    colors {
        background $dark
        separator $dark
        #                       border             background         text
        focused_workspace       $active            $active            $dark
        inactive_workspace      $dark              $dark              $light
        urgent_workspace        $urgent            $urgent            $dark
    }
}